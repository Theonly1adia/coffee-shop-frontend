import PropTypes from 'prop-types';

export default function Hero({ title }) {
  return (
    <div className="Hero">
      <div>
        
      </div>
    </div>
  );
}

Hero.propTypes = {
  title: PropTypes.string.isRequired,
};
